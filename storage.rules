rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Any authenticated member can read media
      allow read: if request.auth != null;
      // Authenticated members can upload media
      allow write: if request.auth != null;
    }
  }
}

